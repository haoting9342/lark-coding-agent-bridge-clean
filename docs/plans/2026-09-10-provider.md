# Feishu private-chat Codex provider switching

Goal: `/provider` reports the current authentication source, `/provider api` and `/provider chatgpt` switch between saved credentials and matching upstream configuration. Only owner/admin private chats may invoke it. No history ID repair or history deletion.

Architecture: separate filesystem provider manager with atomic write, private backups, rollback and fail-closed authentication checks; command layer reuses admin policy and pauses admissions while switching, refusing active work. Stable provider identifier OpenAI. Scope: bridge only, through a dedicated service CODEX_HOME. Preserve inheritCodexHome and catalog fingerprints. User authorized Mac ChatGPT credential initialization.

1. Add failing manager tests for round-trip credentials/config preservation, absent credentials, mismatches, secret-free status, rollback.
2. Implement manager without changing unrelated settings, models or history.
3. Add command tests for group denial, non-admin denial, active-task rejection, status, success and failure.
4. Add command integration and help. Run focused tests, typecheck, build.
5. Provision the approved Mac account; back up live configuration/runtime before deploy. Preserve existing running jobs and unrelated services. Verify target intake and response separately from service-online.

No commits, pushes or PRs authorized.
